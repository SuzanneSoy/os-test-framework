language: c
sudo: false
addons:
  apt:
    packages:
      - qemu
      - qemu-system-x86
      - qemu-system-arm
      - virtualbox
      - bochs
      - sikuli-ide

install:
  - true # Nothing to do for now.

env:
  - MODE=qemu-system-i386
  - MODE=qemu-system-arm
  - MODE=virtualbox
  - MODE=bochs

script:
  - ./test/${MODE}.sh